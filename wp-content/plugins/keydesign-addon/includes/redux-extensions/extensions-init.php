<?php
  // Auto-load extensions placed within the extensions directory
  if( class_exists( 'ReduxFramework' )) {
    // Redux::setExtensions( 'redux_ThemeTek', dirname( __FILE__ ) . '/extensions/' );
    // Extensions are loaded from the theme options-init.php file
  }
